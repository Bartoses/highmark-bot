# HIGHMARK — PROJECT PLAN & ROADMAP

## PRINCIPLES

* Build in small, releasable chunks
* Each feature must be demoable
* Prioritize revenue and onboarding speed
* Keep Claude sessions small to save credits
* Start new Claude session when:

  * Task > 1 feature
  * Context gets messy
  * You switch domains (backend → frontend → infra)

---

## CURRENT STATE (v1)

* AI SMS bot live (Twilio + Claude + Node)
* Booking flows working
* Weather + knowledge base working
* CRM schema exists but campaigns not sending
* Single-client deployment model
* No onboarding flow
* No client dashboard

---

## PHASE 1 — SELLABLE MVP

### 1. Multi-Client Support

Goal: Support multiple businesses in one system

Tasks:

* Create `clients` table (Supabase)
* Route requests by `To` phone number
* Load client config dynamically
* Refactor CLIENT_CONFIG usage

Done when:

* 2+ clients run on same deployment

---

### 2. CRM Campaign Sending

Goal: Enable revenue-driving outbound messaging

Tasks:

* Wire Twilio send into `/crm/campaigns/:id/send`
* Add batching + rate limiting
* Add basic segmentation by tags

Done when:

* Can send campaign to tagged users

---

### 3. Client Onboarding Flow

Goal: Reduce onboarding time to <10 minutes

Tasks:

* Create onboarding form
* Store client config in DB
* Auto-create client record
* Assign Twilio number

Done when:

* New client can be added without code changes

---

### 4. Demo System

Goal: Convert leads via SMS demo

Tasks:

* Improve DEMO trigger flow
* Add scripted responses
* Separate demo vs live behavior

Done when:

* Demo consistently shows value in <60 seconds

---

### 5. Marketing Website

Goal: Establish legitimacy + drive inbound leads

Tasks:

* Landing page
* Pricing tiers
* Demo number CTA
* Value props

Done when:

* Can send link to prospects

---

## PHASE 2 — PRODUCTIZATION

### 6. Admin Dashboard (MVP)

* View conversations
* View contacts
* Basic analytics

---

### 7. Knowledge Base Expansion

* Pull full FareHarbor item details
* Add structured tour + rental data
* Improve answer accuracy

---

### 8. Chat Optimization Loop

* Store conversations
* Identify failures
* Improve prompts

---

### 9. Booking Lookup

* Allow users to check reservations via SMS

---

## PHASE 3 — GROWTH

### 10. Interest Capture

* “Notify me when available”
* Store + trigger campaigns

---

### 11. External APIs

* Snow report
* Trail conditions
* Things to do

---

### 12. Cross-Sell Engine

* Recommend + book other activities

---

## PHASE 4 — SCALE

### 13. Pricing Tiers

* Free
* Starter
* Pro (CRM + campaigns)

---

### 14. Self-Serve Platform

* Auto onboarding
* Billing integration
* Multi-client dashboard

---

## COMPLETED SPRINTS

### Sprint 1 — Multi-Client Portal MVP
- Multi-client architecture, client portal (Supabase Auth + JWT), Conversations viewer,
  Dashboard, Leads, Campaigns, Analytics, Settings, Users & Access, Invites

### Sprint 2A — Conversations Upgrade
- Unified SMS + web chat viewer, pause/resume bot, send-from-portal, AI suggest reply

### Sprint 2B — Dashboard Upgrade
- Hero story cards, conversion funnel, activity feed, calcTimeSaved, ROI banner

### Sprint 2C — Test Console Polish
- Full-width safe/live mode banner, collapsible demo script panel, channel switcher
  (SMS/Web), session state bar, stage badges, system event messages

### Sprint 3A — Leads Table Upgrade (2026-04-17)
- Filter pills (All/New/Contacted/Engaged/Converted/Contacted today)
- Slide-in panel with inline status select, auto-save notes, Mark contacted, View conv
- Channel badge (SMS/Web), `channel` column added to `leads` DB table
- PATCH /portal/api/leads/:id accepts last_contacted_at + channel
- GET /portal/api/leads supports filter=contacted_today

### Sprint 3B — Settings Polish (2026-04-17)
- General tab: 3 visual groups (Business Info / Contact & Notifications / Internal)
  with helper text on every field; added hours + timezone fields
- Booking: 3 plain-English radio cards (FareHarbor / Direct links / Phone only)
  with conditional field visibility
- Bot tab: Preview opener button → in-page preview (TEST_MODE) or Twilio send (live)
- Plan & Usage card: conversations + web sessions this month, usage bar
- POST /portal/api/settings/preview-opener + GET /portal/api/settings/usage

### Sprint 4A — Operator Mode Dashboard (2026-04-23)
- operatorBriefing.js: morning briefing builder (≤960 chars), deterministic SMS command handlers
  (BOOKINGS TODAY/TOMORROW, LEADS, NEW LEADS, WEATHER, REVENUE), portal API data builder
- cron-worker.js: morning briefings run at 7am MT (13:00 UTC window check)
- agentOrchestrator.js: detectAndHandleOperatorCommand() before Claude in owner mode
- index.js: POST /admin/trigger-operator-briefing, GET /admin/operator-briefings,
  GET /portal/api/operator
- portal.html: Operator View section (today's schedule, hot leads, weather, staff placeholder)
  accessible to client_admin role
- db1_operator_briefings.sql: run once in Supabase DB1 to create operator_briefings table

### Sprint 4B — Smart Campaigns (2026-04-23)
- Event-triggered campaigns: snow_fresh / good_weather / opening_date / custom_date
- campaignTriggers.js: evaluateTrigger, evaluateSnowFresh, evaluateGoodWeather, cooldown helper
- POST /admin/simulate-event for preview; campaign_type="event_triggered" on campaigns table

### Sprint 5 — Activity Distribution Network (2026-04-23)
- partner_activities table (DB1): client_id, partner_name, activity_name, category, booking_url,
  seasons text[], keywords text[], commission_pct, enabled, metadata jsonb
- partnerActivities.js: scorePartnerMatch, getActivePartnerActivities, resolvePartnerLink,
  detectSeasonFromContext, buildPartnerActivitiesContext, buildPartnerTrackingUrl
- bookingLinks.js: **Source 5** (confidence 0.60) — activates only when no config/api/crawl match
- knowledgeBase.js: partner context appended to KNOWLEDGE_BASE block (≤12 partners, season-filtered)
- index.js: GET /track/partner (302 + fire-and-forget partner_link_clicked),
  partner_link_sent event when Source 5 wins in SMS
- adminPortal.js: 5 new routes — GET/POST/PATCH/DELETE /portal/api/partners + /analytics
- portal.html: Partners page — cards list, add/edit modal, per-partner CTR analytics
- db1_partner_activities.sql: migration applied to Supabase DB1
- Tests: 34 new assertions covering pure scorers, validators, Source 5 regression, HTTP guards

### Platform Polish — Flexibility + Caching (2026-04-23)
- buildSystemPromptGeneric: industry/season-agnostic builder for non-CSR guided clients
  (driven by client.name, botName, tone, services, bookingUrls, hours, address, faq)
- Dispatcher routes: informational → Informational, id==="csr_rea" → CsrRea (byte-identical),
  all other booking-enabled clients → Generic
- Informational builder: parameterized location + contact name via client.address + primaryContactName
- messageEngine.js: stable system block (persona + rules) wrapped with cache_control: ephemeral;
  volatile block (LIVE DATA + turn plan) sent uncached. ~90% input-token savings on cache hits.
- Uses raw fetch() because @anthropic-ai/sdk@0.20.9 silently drops cache_control; falls back to
  SDK path on any direct-call failure to preserve availability.

### Platform Polish — DRY System Prompt Builders (2026-04-25)
- promptParts.js: shared helpers — smsRulesBlock, contactFailsafeBlock, handoffSection,
  businessInfoBlock, faqBlock, liveDataBlock, formatHours
- buildSystemPromptInformational / Generic / CsrRea now compose via these helpers;
  only domain-specific narrative (CSR snowmobile/RZR/Steamboat blocks, informational
  booking-scheduling block) stays inline. index.js: -106 / +88 lines.
- 1803/1805 tests pass (same 2 pre-existing campaign failures, no regressions).

### Opt-In Name Capture + Crawler Opening Dates (2026-04-28)
- smsOptIn.js: extractNameFromMessage helper (regex + stopword filter) — handles
  "I'm Sean", "my name is Sean Bartosewcz", "call me Sara at 720…", "Sean, 720…".
  buildConsentConfirmReply now asks "What's your name and the best number to text you?"
  so a single reply yields both. Trailing-stopword filter drops "Sara at" → "Sara".
- webChat.js runSmsOptInFlow: extracts name from current turn, falls back to prior
  user turn (visitor often says name, then sends number on its own). Passes `name`
  into saveLead and (firstName/lastName) into upsertContact for CRM.
- crawler.js buildFactExtractionPrompt: now exported; appended ALWAYS-capture
  instruction for opening/closing/season-launch dates regardless of page type.
  Homepage hint widened to include promo banners. New forceReextract option on
  runCrawlerForClient bypasses content_hash so prompt updates can be applied
  without waiting for site content to drift.
- index.js: /portal/api/crawl-now accepts {force:true} body/query → forceReextract.
- Tests: extractNameFromMessage positives/negatives, buildConsentConfirmReply
  asserts name+number, buildFactExtractionPrompt always mentions opening dates +
  homepage hint mentions promo banners. 1984/1986 pass (same 2 pre-existing
  send_campaign failures, no regressions).

### Prompt 7B — Progressive SMS Opt-In (2026-04-28)
- smsOptIn.js: pure helpers — isSmsIntent (detects "text me"/"notify me"/"call me"
  while staying quiet on ambient phone mentions), detectConsentReply (yes/no/unknown),
  detectPhoneInMessage, buildConsentPrompt/ConfirmReply/PhoneCapturedReply/DeclineReply,
  buildSmsFirstTouchOpener.
- webChat.js: runSmsOptInFlow state machine inserted after first-message gate, before
  handoff/Claude. Consent pending → classify reply; opted-in + phone → saveLead +
  CRM tag 'sms_lead' + trackWebEvent("sms_opt_in_captured") + linkSessionToLead;
  not opted-in + isSmsIntent → ask for consent. Existing web→SMS bridgePhone path is
  now gated on convo.smsOptedIn so unconsented phone mentions are ignored.
- conversations row: 3 new columns persisted per session — sms_opted_in,
  sms_opted_in_at, sms_consent_requested. Migration: db1_sms_consent.sql.
- index.js: threads crmSupabase into sendMessageWeb so DB2 contact tagging works.
- Tests: 41 new assertions in testSmsOptIn() — intent positives/negatives, yes/no/
  unknown reply classifier, phone normalization, copy builders include brand + STOP +
  "Msg & data rates may apply". 1969/1971 pass (same 2 pre-existing campaign failures).

### Sprint 7 — Self-Serve Signup + Streamlined Onboarding (2026-04-27)
- selfSignup.js: validateSignupBody, handleSignup, runOnboardingCrawl, handleOnboardingStatus,
  handleOnboardingApprove. Reuses existing onboardingConfig.js pipeline (startAutoConfig,
  commitDraftToDb, getDraft, buildNextSteps).
- Public /signup landing page (public/signup.html): form → POST /api/signup → signInWithPassword
  → redirect /portal/onboarding. Mirrors /portal/login styling, links Supabase via /portal/config.
- /portal/onboarding (public/portal-onboarding.html): polls GET /portal/api/onboarding/status
  every 3s; renders processing/draft/failed/none/approved states. Draft state shows 3-section
  preview (Business / Bot / Booking) + warnings + Approve button.
- index.js: GET /signup, POST /api/signup (rate-limit 10/hr per IP, skipped in TEST_MODE),
  GET /portal/onboarding (registered before /portal/:section catch-all),
  GET /portal/api/onboarding/status, POST /portal/api/onboarding/approve.
- adminPortal.js handlePortalDashboard: extended select to include onboarding_status, bot_mode,
  inbound_phones; computes onboarding_banner (twilio_pending | test_mode); response now carries
  onboarding_status, bot_mode, onboarding_banner.
- public/portal.html loadDashboard: renders banner above checklist (yellow=twilio_pending,
  blue=test_mode).
- Team SMS notify on signup + approve to TEAM_NOTIFY_PHONE (+17202892483).
- db1_onboarding_status.sql: extends onboarding_drafts.status CHECK to include 'processing' and
  'failed'; adds owner_auth_user_id uuid + index, error_message text. Migration is additive;
  must be run once in Supabase DB1 SQL editor.
- Tests: ~40 new assertions in test.js testSelfSignup() — validateSignupBody (10 unit), GET /signup
  (10), GET /portal/onboarding (5), POST /api/signup validation (6), auth guards (3), home regression
  (1). 1928/1930 pass (same 2 pre-existing campaign failures).

### Sprint 6 — Vertical Landing Pages + FAQ Expansion (2026-04-27)
- verticals.js: VERTICAL_SLUGS + VERTICALS config + renderVerticalPage(slug). Three new server-rendered
  pages: /tour-operators, /snowmobile-rentals, /service-businesses. Each has unique meta, canonical,
  hero, dual-channel SMS + Web Chat demo conversations, 3-item industry FAQ, and FAQPage +
  SoftwareApplication JSON-LD. Shared nav, pricing (mirrors home.html), demo band, footer.
- index.js: routes registered via for-loop over VERTICAL_SLUGS.
- public/home.html: Use-Cases cards now anchor links to the matching vertical; "See how it works →"
  cta on every card; restyled to be focusable links.
- public/sitemap.xml: 3 new URLs at priority 0.8 (changefreq monthly).
- test.js: ~85 new assertions covering route status, meta/og/canonical/JSON-LD, dual-channel demo
  presence, pricing section, sitemap inclusion, homepage link-out, JSON-LD parse validity.

### Platform Polish — /sms Refactor + Latency (2026-04-24)
- /sms handler body extracted from index.js (2953 → 2063 lines) to smsOrchestrator.js (944 lines).
  index.js keeps a 3-line delegate; ESM live bindings handle the circular import. Byte-identical behavior.
- Parallelized independent Supabase reads in /sms: checkOptOut + getRuntimeClientConfig +
  getConversation fire together at step 5 (~300ms off common path); opt-out awaits first, others
  downstream as needed. Orphaned reads get .catch(() => {}) on the opt-out short-circuit.
- dateExtract.js: deterministic pure-JS date parser replaces the per-message Claude call in
  booking-step null→1. Covers today/tomorrow/day-after-tomorrow/tonight, weekday names (+ "next Friday"),
  this/next weekend, MM/DD(/YY(YY)), YYYY-MM-DD, "December 15" / "15 December" / "3rd of January".
  Past dates without explicit year roll forward. 32 new unit tests against a fixed Monday anchor.

### DB2 Source of Truth + Polaris Confirmations (2026-05-21)
Owner asks: booking data should always come from DB2 (real CRM manifest, not
FH availability cache), and Polaris/MPWR bookings need confirmation texts
gated by the existing portal toggle.

- operatorBriefing.js buildOperatorApiData (used by GET /portal/api/operator
  → portal Operator View): now fetches DB2 daily_manifest first, falls back
  to FH availability KB only when no crmSupabase is configured. Returns
  normalized `bookings: [{ time, name, capacity, pk, revenue_cents, source }]`
  + a top-level `bookings_source` ∈ {manifest, manifest_empty, fh_kb} so the
  UI can label data provenance.
- index.js: GET /portal/api/operator now threads crmSupabase into
  buildOperatorApiData.
- mpwrSync.js: new sendPolarisConfirmationIfNeeded + buildPolarisConfirmationText.
  After every successful booking upsert, the sync optionally fires a Twilio
  confirmation text gated by messaging_config.enable_confirmation_texts for
  csr_rea (default OFF). Idempotent — DB1 confirmations_sent stamps source='mpwr'.
  Test-mode honors CONFIRMATIONS_ENABLED + CONFIRMATIONS_TEST_PHONE.
  Confirmation text format:
    "Hey {first}! Your {Kremmling|Steamboat} RZR ride with Colorado Sled
    Rentals is confirmed for {Monday, July 4} from 9am–1pm 🏔
    Confirmation: CO-ABC-123. Reply here with any questions!"
- cron-worker.js: runMpwrSync now passes db1 (DB1 supabase) + twilioClient so
  the confirmation path can resolve config + send SMS.
- public/portal.html: "Booking Confirmations" toggle help text updated to
  clarify it covers both FareHarbor and Polaris (RZR) bookings.
- Tests: +10 assertions in testPolarisConfirmations — first-name fallback,
  location label, company name, formatted date, time range, booking PK
  inclusion, length cap, name-missing graceful path, missing-time
  graceful path. 2326/2326 passing.

### Briefing Polish — Real Manifest + Season Revenue + New Bookings (2026-05-21)
Owner feedback after the first live preview of the briefing: "Today's bookings"
was reading from the FH availability cache (which shows OPEN SLOTS, not actual
bookings) and the empty-state copy referenced FareHarbor directly when summer
operations run through Polaris/MPWR. Also missing: season revenue and a "new
bookings created" pulse for the owner to see how fast the funnel is filling.

- operatorBriefing.js:
  • New getTodaysManifest(clientId, crmSupabase) — DB2 daily_manifest scoped
    to today's start_at, sorted ascending. Returns real bookings (FH + MPWR
    via the underlying bookings table).
  • New getSeasonRevenue(client, crmSupabase, supabase, season?) — current-
    season window (Summer = May 1 → Oct 31; Winter = Nov 1 → Apr 30 of next
    year). DB2 sum preferred; DB1 leads estimate fallback ($175 avg).
  • New getNewBookingsStats(crmSupabase, supabase) — counts bookings created
    in the last 24h / 7d. Tries DB2 bookings.created_at first (covers FH +
    MPWR inserts), falls back to DB1 confirmations_sent.confirmation_sent_at
    on error.
  • buildBriefingText rewritten to lead with TODAY (real manifest count +
    pax + revenue subtotal + first 5 bookings by time), NEW BOOKINGS section
    (suppressed when zero), HOT LEADS, CONDITIONS, 7-DAY + season revenue
    lines, ALERTS, numeric menu. Empty-state copy is now "Nothing on the
    manifest yet — quiet day so far" — no platform references.
  • formatManifestResponse (exported) — shared formatter used by the
    "bookings today / tomorrow" handlers when crmSupabase is available.
    Falls back to formatBookingsResponse (FH KB availability) when no CRM.
  • Briefing length cap raised from 960 → 1280 to accommodate the new
    sections without truncating.
- generateDailyBriefing + handlePortalPreviewBriefing: parallelized fetches
  now include manifest + seasonRevenue + newBookings.
- Tests: +24 assertions in testBriefingPolish — manifest header, listed
  bookings, NEW BOOKINGS line, SEASON line, 7-DAY line, no-jargon
  empty-state, formatManifestResponse today/tomorrow paths,
  getSeasonRevenue manifest + estimate fallback paths,
  getNewBookingsStats db2 happy path + db1_confirmations fallback,
  getTodaysManifest graceful-null. 2316/2316 passing.

### Sprint C — Operator Intelligence: Portal UI for Per-Phone Tuning (2026-05-21)
Third sprint of the Daily Operator Briefing upgrade: ships the portal UI that
lets client_admins manage per-phone digest preferences, send test briefings,
and preview today's briefing — completing the multi-tenant operator config
loop that Sprints A and B set up.

- adminPortal.js: 6 new handlers — handlePortalOperatorPhones (list),
  handlePortalCreateOperatorPhone (POST), handlePortalUpdateOperatorPhone
  (PATCH), handlePortalDeleteOperatorPhone (DELETE),
  handlePortalTestOperatorPhone (POST :id/test → generateDailyBriefing with
  dedup bypass), handlePortalPreviewBriefing (GET, builds briefing text
  without sending).
- validateOperatorPhoneInput: shared validator for POST/PATCH — normalizes
  E.164 phone, enforces role ∈ {owner,manager,sales,staff}, digest_times
  HH:MM 24-hour, digest_types ⊆ {all,bookings,revenue,leads,staffing,issues,
  weather}, timezone via Intl.DateTimeFormat. 409 on unique-violation, 403
  on cross-tenant, 404 on missing row.
- index.js: 6 new routes wired under requirePortalAuth — GET/POST/PATCH/
  DELETE /portal/api/operator-phones[/:id], POST /:id/test, GET
  /portal/api/operator/preview-briefing.
- syncOperatorPhonesTable in the Settings save handler no longer deletes
  rows missing from the legacy clients.operator_phones list. The Sprint C
  UI can add phones that aren't mirrored to the JSONB column, and removal
  is now an explicit DELETE through the new endpoint.
- public/portal.html: new "🤖 Operator Intelligence" card below the existing
  Internal section. Per-phone cards expose label, role, internal-mode +
  daily-digest toggles, digest_times comma-input, sections checkbox grid,
  timezone, last_digest_sent_at, "Send test now" button, Save button,
  Remove button. Top-of-card "Preview today's briefing" renders the briefing
  text in a scrollable monospace box. loadOperatorIntelligence() is called
  from loadSettings() so the section populates whenever the user opens
  Settings.
- Tests: +18 assertions in testSprintCOperatorPortal — 401 guards on all
  routes, POST validation (bad phone, role, digest_times, digest_types,
  timezone), POST happy path (E.164 normalization + array persistence),
  PATCH partial update + cross-tenant 403 + not-found 404 + empty-body
  400, DELETE happy path, client_user 403 on POST. 2292/2292 passing.

### Sprint B — Operator Intelligence: Conversational UX + Expanded Handlers (2026-05-21)
Second sprint of the Daily Operator Briefing upgrade: turns the briefing into a
genuine two-way thread. Numeric quick-reply menu, separate operator
conversation type, and 5 new freeform handlers backed by DB1 leads + DB2
daily_manifest.

- operatorBriefing.js:
  • buildBriefingText footer replaced with a 7-row numeric menu
    (1 Today's manifest / 2 Revenue / 3 Open leads / 4 Ops load /
     5 Weather / 6 Tomorrow outlook / 7 Unanswered convos) + "or ask anything".
  • Exported OPERATOR_MENU + resolveMenuShortcut("1"–"7"). The
    detectAndHandleOperatorCommand entry point resolves the shortcut to its
    canonical string BEFORE routing, so numeric replies traverse the exact
    same path as natural-language commands.
  • Two new menu-backed handlers: formatOpsLoadResponse (issues + hot leads
    + manifest count) and formatUnansweredResponse (handoff/bot_paused
    conversations).
  • Five new freeform handlers — formatLargestBookings (DB2 daily_manifest
    ORDER BY total_cents), formatUnderbookedActivities (next 14 days, group
    by activity), formatBusiestHours (group by hour-of-day MT),
    formatNeedsFollowup (DB1 leads contacted >24h ago or never contacted),
    formatFirstTimers (per-phone total booking count = 1).
  • formatHelpResponse rewritten to lead with the numeric menu + list the
    new freeform queries.
- db1_conversation_type.sql: ALTER conversations ADD COLUMN conversation_type
  TEXT NOT NULL DEFAULT 'guest' CHECK IN ('guest','internal_operator') +
  composite index on (client_id, conversation_type). Migration applied.
- index.js: saveConversation tags rows with conversation_type='internal_operator'
  when convo.conversationType is set; falls back gracefully if the column
  doesn't exist yet (catches "conversation_type" in the error message and
  retries without it). getConversation hydrates convo.conversationType from
  the DB row.
- smsOrchestrator.js: after detectOwner resolves, stamps
  convo.conversationType = "internal_operator" on the awaited convo so all
  downstream saveConversation calls persist the tag.
- Tests: +40 assertions in testSprintBOperatorUX — shortcut routing (1–7,
  whitespace, invalid), briefing menu rendering, ops load + unanswered
  formatting, largest bookings dollar formatting + CRM-missing path,
  underbooked sort order, busiest hours peak detection, follow-up
  age-since-contact + "never contacted", first-timers per-phone count
  exclusion. 2274/2274 passing.

### Sprint A — Operator Intelligence: Multi-Tenant Operator Phones (2026-05-21)
First sprint of the Daily Operator Briefing upgrade: replaces the static
`clients.owner_phone` + `clients.operator_phones` JSONB with a normalized
`operator_phones` table that supports per-phone preferences and per-time
dispatch. Foundation for Sprints B (conversational UX + numeric menus) and
C (portal Settings → Internal UI).

- db1_operator_phones.sql: `operator_phones` table — id, client_id (FK), phone
  (E.164, unique per client), label, role (owner|manager|sales|staff),
  internal_mode_enabled, daily_digest_enabled, digest_times text[] (HH:MM in
  timezone), digest_types text[] (all|bookings|revenue|leads|staffing|issues|
  weather), timezone, last_digest_sent_at, updated_at trigger. Idempotent
  backfill: clients.owner_phone → role=owner, clients.operator_phones[] →
  role=staff. Migration applied to DB1 (csr_rea: 1 owner + 4 staff,
  highmark_demo: 1 owner).
- clientConfig.js: fetchOperatorPhones() per-request fetch with module-level
  flag for graceful "table missing" fallback. When rows exist, they are
  AUTHORITATIVE — derives merged.ownerPhone (role=owner row only) and
  merged.operatorPhones[] (internal_mode_enabled=true only). Adds
  merged.operatorPhoneRows for downstream consumers. Falls back to legacy
  columns when table empty.
- adminPortal.js: syncOperatorPhonesTable() — after the Settings save writes
  owner_phone / operator_phones JSONB, mirror into the table:
  UPSERT new phones (preserving existing per-phone preferences), DELETE rows
  removed from the legacy set, re-tag role (owner row demoted to staff if
  ownerPhone changed).
- operatorBriefing.js:
  • generateDailyBriefing now accepts options { toPhone, dedupKey, opRowId,
    digestTypes }. Per-phone dedup via __DEDUP:{key}__ trailer in
    operator_briefings.content; legacy callers (no options) keep the
    original "one briefing per client per 20h" semantics.
  • New isDigestTimeNow(digestTimes, timezone, now) — pure helper that
    matches a 5-min cron window against HH:MM slots in the row's timezone.
  • New dispatchOperatorDigests(supabase, twilio, crmSupabase, now) —
    reads operator_phones rows, filters by daily_digest_enabled +
    isDigestTimeNow, groups by client_id, fans out per-row briefings,
    stamps last_digest_sent_at on success.
- cron-worker.js: replaced isBriefingWindow() + sendOperatorBriefings()
  with a single dispatchOperatorDigests() call on every cron tick. Each
  operator_phones row decides its own due time, so multi-time digests
  (06:30 + 13:00 + 19:00) work without a global window check.
- Tests: +19 assertions in testSprintAOperatorPhones — isDigestTimeNow
  positives/negatives/timezone honoring, dispatch happy path, missing-table
  fallback, detectOwner with config-derived phones, runtime config
  derivation rules. 2234/2234 passing.

### Phase X — Operator Intelligence (2026-05-04)
Three-part upgrade so the owner/operator interface acts like an ops assistant rather than
a database tool. Every reply is fast, includes one operator-facing insight, and never
exposes DB internals or "no records" copy.

- (a) ownerMode.buildOwnerInstruction rewritten with the Phase X spec: two-layer
  awareness, context-inheritance rules, intent mapping (kremmling → location;
  REA / Rabbit Ears / CSR → company), empty-state hard rules (never "no records",
  never expose Supabase / CRM / tables), required one-line insight, Phase-X bullet
  template, seasonal business context (Summer=RZRs, Winter=snowmobiles, Kremmling
  primary). Null-fallback also hides DB references.
- (b) Operator context memory across turns. operatorIntentParser.js exports
  isVagueFollowUp / mergeOperatorContext / extractOperatorContext. FILLER_STARTS
  extended with and/or/but/& to fix spurious company_filter extraction on
  metric-only refinements. operatorBriefing.detectAndHandleOperatorCommand
  accepts an optional convo arg: reads prior context off booking_data._operator,
  merges it into the parsed intent, persists the new context after each
  successful structured route. Bare-revenue intercept defers to the structured
  path when inherited context provides a date_range. agentOrchestrator.js threads
  convo through. "kremmling" / "what about steamboat?" / "and revenue?" inherit
  timeframe + entity + metric from the prior turn.
- (c) Fallback intelligence + insight + Phase-X format (actionEngine.js).
  buildOperatorInsight (exported pure helper) picks one insight per response from
  a strategy ladder: location dominance → barely-contributing 2nd → company
  dominance → small-group pattern → top activity. formatPhaseXBookingReply
  renders {Scope} header, • Bookings / Guests / Revenue bullets, insight,
  optional By location: / By company: / Top activities:. handleGetBookingsByDateRange
  retry ladder on 0 rows: drop location → drop company → broaden to last 90 days,
  each level prepending "I'm not seeing X — here's Y:". Truly-empty state suggests
  a campaign instead of "no records". handleReport adopts the same bullet format
  + insight in result.insight. All "I couldn't find any records … adjust the
  timeframe or filters" copy removed.

Tests: +62 assertions in test.js — 2138/2138 passing. Covers the new ownerMode
rules, isVagueFollowUp / merge / extract, convo persistence + inheritance round-trip,
buildOperatorInsight branches, fallback retry, Phase-X format on bookings + report
handlers.

---

## NEXT 5 BUILDS (STRICT ORDER)

1. Sprint 3C — Conversations upgrade (search, filter by phone, bulk pause)
2. Sprint 3D — Campaigns v2 (send history, delivery stats)
3. Sprint 6 — Partner onboarding UX (bulk CSV import, auto-season from keywords)
4. Analytics deep-dive (per-channel funnel, lead source attribution)
5. Billing & plan enforcement (message limits, upgrade flow)

---

## CLAUDE CODE RULES

For every task:

1. Keep scope small (1 feature only)
2. Write tests
3. Test locally
4. Deploy
5. Verify end-to-end
6. Update docs

Never:

* Bundle multiple features in one prompt
* Skip testing
* Break existing flows

---

## SUCCESS METRIC

You can:

* Add a new client in <10 min
* Demo in <60 sec
* Send campaigns
* Charge $300+/mo per client

---
